#include <cstdio>
#include <iostream>
#include <string>

using namespace std;

int main(void){ 
    string a, b;
    cin >> a >> b;
    
    int j = 0;
    for(int i =0 ;i < a.size();i++) {
        for(;j < b.size();) {
        }
    }
    cout << a << endl << b;
    return 0;
}