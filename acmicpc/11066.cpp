#include <csttdio>

int main(void){ 
    int t;
    scanf("%d", &t);
    while(t--) {
        int k;
        int arr[512];
        scanf("%d", &k);
        for(int i = 0 ;i < k;i++) {
            scanf("%d", arr + i);
        }
    }
    return 0;
}